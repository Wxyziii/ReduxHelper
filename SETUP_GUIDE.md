# Setup Guide

Required: Node.js, Rust/Tauri toolchain for desktop development.

Optional tools:
- OpenRouter API key for text AI.
- Microsoft texconv for DDS conversion.
- Local ComfyUI for PNG texture image AI.

Run:
```powershell
npm install
npm run tauri dev
```
